#include <iostream>  //A must for input and output otherwise it's just dumb
#include <string>    //Isn't strictly required but good practice for string manipulation
#include <vector>    //In order to use the vector
#include <algorithm> //In order to use the sorting function

// Not gonna use #define ll long long cause the question states the username will be within 1 to a 100 characters.

using namespace std; // For readability and clarity

void fast_io() // For faster input and output otherwise there might arise complications during real contests
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
}

int main()
{
    

    return 0;
}